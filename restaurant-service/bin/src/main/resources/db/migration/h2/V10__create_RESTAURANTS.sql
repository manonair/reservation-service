CREATE TABLE RESTAURANTS (
  RESTAURANT_ID integer PRIMARY KEY,
  RESTAURANT_NAME VARCHAR(255),
  STATUS VARCHAR(10) 
);

CREATE TABLE MENU (
  MENU_ID integer PRIMARY KEY,
  ITEM_NAME VARCHAR(255),
  ITEM_DESC VARCHAR(255),
  PRICE integer,
  STATUS BOOLEAN
  );


CREATE TABLE TABLES (
  TABLE_ID integer PRIMARY KEY,
  TABLE_NAME VARCHAR(255),
  TABLE_DESC VARCHAR(255),
  CAPACITY INTEGER,
  STATUS BOOLEAN,
  BOOKING_START TIMESTAMP,
  BOOKING_END TIMESTAMP
  );

CREATE TABLE RESTAURANTS_TABLES (
	RESTAURANTS_TABLE_ID integer PRIMARY KEY,
   RESTAURANT_ID integer NOT NULL,
   TABLE_ID integer NOT NULL,
    STATUS BOOLEAN,
  BOOKING_START TIMESTAMP,
  BOOKING_END TIMESTAMP
);

ALTER TABLE RESTAURANTS_TABLES ADD CONSTRAINT RESTAURANTS_TABLES_TABLE
   FOREIGN KEY (TABLE_ID) REFERENCES TABLES;

ALTER TABLE RESTAURANTS_TABLES ADD CONSTRAINT RESTAURANTS_TABLES_RESTAURANT
   FOREIGN KEY (RESTAURANT_ID) REFERENCES TABLES;
  